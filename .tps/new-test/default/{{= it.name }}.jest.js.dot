/*
 * Modules
 */
import _ from '@tps/_';

/*
 * Constants
 */

describe('_', () => {
  beforeAll(() => {});
  afterAll(() => {});
  beforeEach(() => {});
  afterEach(() => {});

  it('_', () => {});
});
