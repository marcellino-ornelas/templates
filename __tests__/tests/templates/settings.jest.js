/*
 * Modules
 */

import templates from '@test/templates';

/*
 * Constants
 */


describe('[TPS] _', () => {
  let tps;

  beforeAll(() =>  );
  afterAll(() =>  );

  beforeEach(() => {
     tps = new Templates();
    
  });

  it('should', () => {});
});
